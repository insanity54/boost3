'use strict';

/**
 * bot-paypal-token controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::bot-paypal-token.bot-paypal-token');
